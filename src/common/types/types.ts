export type NewPainting = {
  // id: string;
  name: string;
  imageUrl: string;
  paintedYear: string;
  // type: string;
  // description: string;
  // techniques: string[];
  // size: string;
  // availability: boolean;
  // price: string;
};

export type DBPainting = {
  id: string;
  name: string;
  imageUrl: string;
  paintedYear: string;
  // type: string;
  // description: string;
  // techniques: string[];
  // size: string;
  // availability: boolean;
  // price: string;
};

export type Credentials = {
  email: string;
  password: string;
};
